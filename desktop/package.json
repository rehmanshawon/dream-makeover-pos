{
  "name": "@dream-makeover/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop shell for Dream Makeover POS",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "echo \"No desktop tests yet\" && exit 0",
    "build": "tsc"
  }
}
