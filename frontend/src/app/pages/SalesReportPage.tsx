import type { JSX } from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export function SalesReportPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Sales Report"
      description="Detailed sales history with filters and export options."
    />
  );
}
