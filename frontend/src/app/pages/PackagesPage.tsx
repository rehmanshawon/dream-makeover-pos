import type { JSX } from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export function PackagesPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Packages"
      description="Create and manage bundles of services and products."
    />
  );
}
