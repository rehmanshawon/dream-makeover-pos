import type { JSX } from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export function SettingsPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Settings"
      description="System configuration, users, and printer setup."
    />
  );
}
