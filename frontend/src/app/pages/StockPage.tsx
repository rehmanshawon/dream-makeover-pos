import type { JSX } from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export function StockPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Stock / Inventory"
      description="Stock levels, stock-in, adjustments, and low-stock alerts."
    />
  );
}
